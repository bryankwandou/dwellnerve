import {AppShell} from "@/components/app-shell"; import {SolanaProofButton} from "@/components/solana-proof-button"; import {ledger} from "@/lib/demo-data";
export default function Page(){return <AppShell eyebrow="Lease-aware ledger" title="Money follows the signed terms" description="Fees appear only after grace and never above the configured jurisdiction cap."><section className="statGrid compact"><article className="statCard"><span>Scheduled</span><strong>$85,095</strong><p>Current cycle</p></article><article className="statCard"><span>Received</span><strong>$82,440</strong><p>96.8% collected</p></article><article className="statCard"><span>At risk</span><strong>$2,185</strong><p>One lease past threshold</p></article></section><section className="panel tablePanel"><div className="panelHead"><div><p className="eyebrow">July 2026</p><h2>Rent ledger</h2></div><SolanaProofButton event="july-ledger-reconciled"/></div><div className="dataTable"><div className="tableRow tableHead"><span>Tenant</span><span>Rent</span><span>Lease fee</span><span>Status</span></div>{ledger.map(x=><div className="tableRow" key={x.tenant}><span><strong>{x.tenant}</strong><small>{x.unit} · {x.date}</small></span><span>${x.rent.toLocaleString()}</span><span>{x.fee?`$${x.fee}`:"—"}</span><span><b className={`status ${x.status}`}>{x.status}</b></span></div>)}</div></section></AppShell>}