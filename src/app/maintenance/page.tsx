import {AppShell} from "@/components/app-shell"; import {SolanaProofButton} from "@/components/solana-proof-button"; import {maintenance} from "@/lib/demo-data";
export default function Page(){return <AppShell eyebrow="Maintenance nerve" title="Risk-ranked work, not a shared inbox" description="Emergency language is evaluated before routing and notification."><section className="panel tablePanel"><div className="panelHead"><div><p className="eyebrow">Live queue</p><h2>12 open requests</h2></div><SolanaProofButton event="maintenance-queue-reviewed"/></div><div className="dataTable"><div className="tableRow tableHead"><span>Risk</span><span>Request</span><span>Trade</span><span>Waiting</span></div>{maintenance.map(x=><div className="tableRow" key={x.id}><span><b className={`status ${x.urgency}`}>{x.urgency}</b></span><span><strong>{x.issue}</strong><small>{x.id} · {x.unit} · {x.tenant}</small></span><span>{x.trade}</span><span>{x.age}</span></div>)}</div></section></AppShell>}