import { Connection, Keypair, LAMPORTS_PER_SOL } from "@solana/web3.js";
import { NextResponse } from "next/server";
import bs58 from "bs58";
export async function GET() { try { const connection = new Connection(process.env.NEXT_PUBLIC_SOLANA_RPC_URL || "https://api.devnet.solana.com", "confirmed"); const [version, slot] = await Promise.all([connection.getVersion(), connection.getSlot()]); let signer = null; if (process.env.SOLANA_DEVNET_SECRET_KEY) { const keypair = Keypair.fromSecretKey(bs58.decode(process.env.SOLANA_DEVNET_SECRET_KEY)); const balance = await connection.getBalance(keypair.publicKey); signer = { publicKey: keypair.publicKey.toBase58(), balanceSol: balance / LAMPORTS_PER_SOL }; } return NextResponse.json({ network: "devnet", healthy: true, slot, version: version["solana-core"], signer }); } catch (error) { return NextResponse.json({ network: "devnet", healthy: false, error: error instanceof Error ? error.message : "RPC unavailable" }, { status: 503 }); } }