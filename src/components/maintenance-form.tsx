"use client"; import {useState} from "react"; import {ArrowRight,Camera,Siren} from "lucide-react";
type TriageResponse={urgency:string;trade:string;reason:string;notified:boolean};
export function MaintenanceForm(){const [description,setDescription]=useState("Strong gas smell near the kitchen range.");const [result,setResult]=useState<TriageResponse|null>(null);const [loading,setLoading]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);const r=await fetch("/api/maintenance/triage",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({unit_id:"briar-2b",raw_description:description,photo_paths:[]})});setResult(await r.json());setLoading(false)}return <div className="splitPanel"><form className="formCard" onSubmit={submit}><label>What is happening?<textarea value={description} onChange={e=>setDescription(e.target.value)} minLength={8} required/></label><label className="upload"><Camera size={19}/>Add photos or video<input type="file" accept="image/*,video/*" multiple/></label><button className="primaryButton" disabled={loading}>{loading?"Checking risk…":"Submit and route"}<ArrowRight size={17}/></button></form><div className={`triageResult ${result?.urgency==="emergency"?"emergencyCard":""}`}><p className="eyebrow">Live triage result</p>{result?<><Siren size={28}/><h2>{result.urgency}</h2><strong>{result.trade} dispatch</strong><p>{result.reason}</p><span>{result.notified?"Manager and coordinator notified in-request":"Queued for review"}</span></>:<><h2>Risk first. Routing second.</h2><p>DwellNerve checks for safety and property-loss language before choosing the trade queue.</p></>}</div></div>}